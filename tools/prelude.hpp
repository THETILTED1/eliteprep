// What the judge has already included by the time your solution is compiled.
//
// Forced in front of every file by .clangd, so the source stays paste-ready:
// no includes, no `using namespace std;`, no `std::`. Nothing else belongs
// here — the helper types (ListNode, TreeNode, the several shapes of Node,
// Interval) are already in the files that need them, commented out as LeetCode
// ships them, and tools/stitch.py uncomments those on the way past. Defining
// them here instead would redefine them and pin Node to one shape.
//
// Spelled out rather than <bits/stdc++.h>, which is a libstdc++ extension and
// does not exist on libc++ or MSVC.
#pragma once

#include <algorithm>
#include <array>
#include <bit>
#include <bitset>
#include <cassert>
#include <cctype>
#include <climits>
#include <cmath>
#include <compare>
#include <concepts>
#include <cstddef>
#include <cstdint>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <deque>
#include <forward_list>
#include <functional>
#include <initializer_list>
#include <iomanip>
#include <ios>
#include <iostream>
#include <istream>
#include <iterator>
#include <limits>
#include <list>
#include <map>
#include <memory>
#include <numeric>
#include <optional>
#include <ostream>
#include <queue>
#include <random>
#include <ranges>
#include <set>
#include <span>
#include <sstream>
#include <stack>
#include <stdexcept>
#include <string>
#include <string_view>
#include <tuple>
#include <type_traits>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <variant>
#include <vector>

using namespace std;
